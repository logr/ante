<?xml version="1.0" encoding="utf-8"?>
<!-- No `package` attribute: the namespace comes from android.namespace in build.gradle.kts. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- No android:allowBackup: the catalog holds no user data, and setting it either way only
         earns a DataExtractionRules deprecation note. -->
    <application
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnteCatalog">

        <activity
            android:name=".CatalogActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
